# Stage baseline — 2026-08-01

Measured by `scripts/benchmark.mjs 500` on node v24.15.0.
Hermetic: generated photos in a temp dir, index in a temp AUTOGALLERY_HOME.

This is the **before** half of decision D2 (the unified pipeline must be at
least as fast as today's separate passes). Re-run the same command after
Phase 3 and diff.

| stage            | total | per unit                                           |
| ---------------- | ----- | -------------------------------------------------- |
| `generate`       | 565ms | 1.13ms/photo                                       |
| `index`          | 2ms   | 0.00ms/photo                                       |
| `meta`           | 79ms  | 0.16ms/photo                                       |
| `hash`           | 31ms  | 0.06ms/photo                                       |
| `pipeline`       | 42ms  | 0.08ms/photo                                       |
| `seed-rows`      | 500ms | 0.00ms/photo                                       |
| `cohort (first)` | 0ms   | 0.01ms — everything pending, LIMIT stops at row 57 |
| `cohort (last)`  | 367ms | 7.34ms — full scan of 125,000 rows                 |

**Cohort query plan:** `SCAN photos | SEARCH folders USING INTEGER PRIMARY KEY (rowid=?)`

**D2 gate:** PASS — pipeline 42ms vs separate 110ms

`embed` and `faces` are NOT in this table — they need real weights and were skipped. Re-run with `ML_INTEGRATION=1` to include them.
