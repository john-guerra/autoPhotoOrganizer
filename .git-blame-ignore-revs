# Revisions to ignore in `git blame` — bulk reformatting / whitespace-only
# commits that would otherwise obscure real authorship.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# (GitHub's blame view honours this file automatically.)

# style: prettier baseline across the repo (#76)
85a0016fa2de460accc7aaea7e1b29b608810309
